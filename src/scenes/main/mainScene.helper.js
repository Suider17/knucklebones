export function setRoundText(scene) {
  scene.text.roundCounter.setText(scene.props.round);
}
export function setUntilDuelText(scene) {
  console.log();
  scene.text.untilDuelCounter.setText(scene.props.untilDuelCounter);
}
